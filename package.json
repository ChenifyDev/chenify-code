{
    "name": "chenify-code",
    "version": "0.0.0",
    "private": true,
    "workspaces": ["apps/*", "packages/*"],
    "scripts": {
        "dev:web": "bun run --filter '@chenify/web' dev",
        "dev:server": "bun run --filter '@chenify/server' dev",
        "build": "bun run --filter '@chenify/web' build",
        "lint": "oxlint",
        "format": "oxfmt"
    },
    "devDependencies": {
        "oxfmt": "^0.62.0",
        "oxlint": "^1.77.0"
    }
}
